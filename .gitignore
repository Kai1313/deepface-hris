# DeepFace service env (copy from .env.example; may hold the auth token — never commit)
.env

# Python
__pycache__/
*.pyc
*.pyo

# HRIS runtime artifacts (contains face embeddings = biometric data — never commit)
HRIS/hris.db
HRIS/hris.log

# OS junk
.DS_Store
Thumbs.db
